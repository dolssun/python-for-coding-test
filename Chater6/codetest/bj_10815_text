# [입력 방법]
1. input 함수 사용
    a = int(input())

2. import sys
    (1) import sys

        input = sys.stdin.readline
        a = int(input())

    (2) import sys

        a = int(sys.stdin.readline())

## [set]

1. set은 중복 허용 x
2. set은 메서드 sort() 사용 불가 -> sort()는 리스트
    # sort()는
        - list 전용 메소드 이므로, 리스트이름.sort() 로 사용
        - 복사본을 만들지 않아 sorted()보다 속도가 빠름
        - 원본값 직접 수정됨
3. 정렬을 원할경우 파이썬 내장함수 sorted() 사용

### [if .. in ..]

1. 배열의 값이 존재하는지 비교할때 : in 을 이용해서 확인할 수 있다

