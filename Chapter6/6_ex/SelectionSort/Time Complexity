<선택 정렬 시간 복잡도>

1. 선택정렬은 N-1번 만큼 가장 작을 수를 찾아 맨앞으로 보낸다.
2. 매번 가장 작은 수를 찾기 위해 비교연산이 필요하다.

* 연산횟수
> N+(N-1)+(N-2)+...+2 ≈ NX(N+1)/2 = (N^2+N)/2 = O(N^2)
> 소스코드상, 간단한 형태의 2중 반복문이 사용되었다.

* 예시)
> 정렬할 데이터 개수가 100배 늘어남 -> 수행시간은 10,000배 늘어난다.

* 결론
> 데이터 개수가 증가할 수록 비효율적
> 데이터 개수가 적을수록 좋음
> 특정길이의 리스트에서 데이터를 찾는 일이 코딩테스트에서 잦으므로 선택정렬에 익숙해지자
